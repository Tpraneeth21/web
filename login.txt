create database login ;

use login;

 CREATE TABLE users (
    ->     username VARCHAR(50) NOT NULL,
    ->     NIC_NO INT NOT NULL,
    ->     email VARCHAR(100) NOT NULL,
    ->     C VARCHAR(255) NOT NULL
    -> );
